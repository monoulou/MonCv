{% extends "::base.html.twig"%}

{% block body %}

  <h3>Formation et diplômes</h3>


  <div class="col-md-12 table-responsive">
     <table class="table table-bordered table-striped table-condensed">
            
        <thead>
          <tr>
            <th>Années</th>
            <th>Diplômes</th>
          </tr>
        </thead>

        <tbody>
          {% for formation in formationList %}
          <tr>
            <td>{{ formation.dateFormation}}</td> 
            <td>{{ formation.intitule }}</td>
          </tr>
          {% endfor %}
        </tbody> 
      </table>
  </div>




<!--
      {% for formation in formationList %}
        Années : {{formation.dateFormation}} | {{formation.intitule}}.<br>
      {% endfor %}
-->

{% endblock %}
